<UserControl x:Class="JPSoftworks.EditorBar.Controls.ColorButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JPSoftworks.EditorBar.Controls"
             xmlns:presentation="clr-namespace:JPSoftworks.EditorBar.Presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <presentation:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    </UserControl.Resources>
    <Button VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Click="ButtonBase_OnClick">
        <StackPanel Orientation="Horizontal">
            <Label Content="{Binding Label, RelativeSource={RelativeSource AncestorType=local:ColorButton}, Mode=OneWay}"></Label>
            <Border CornerRadius="1" MinWidth="32" MinHeight="20" BorderThickness="1" Background="{Binding SelectedColor, Converter={StaticResource ColorToBrushConverter}, RelativeSource={RelativeSource AncestorType=local:ColorButton}, Mode=OneWay}" />
        </StackPanel>
    </Button>
</UserControl>
